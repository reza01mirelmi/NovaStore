# NovaStore
NovaStore is a backend project built with Node.js, Express, and MongoDB using MVC architecture. It provides a clean, modular structure with RESTful APIs for managing users, products, orders, authentication, validation, and error handling 
